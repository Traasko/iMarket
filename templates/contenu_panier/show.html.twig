{% extends 'base.html.twig' %}

{% block title %}ContenuPanier{% endblock %}

{% block body %}
    <h1>ContenuPanier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contenu_panier.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ contenu_panier.quantite }}</td>
            </tr>
            <tr>
                <th>Ajout</th>
                <td>{{ contenu_panier.ajout ? contenu_panier.ajout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contenu_panier_index') }}">back to list</a>

    <a href="{{ path('app_contenu_panier_edit', {'id': contenu_panier.id}) }}">edit</a>

    {{ include('contenu_panier/_delete_form.html.twig') }}
{% endblock %}
