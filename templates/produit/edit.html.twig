{% extends 'base.html.twig' %}

{% block title %}{{'edit produit'|trans}}{% endblock %}

{% block body %}
    <h1>{{'edit produit'|trans}}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_produit_index') }}" class="btn btn-secondary">{{'back to list'|trans}}</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
